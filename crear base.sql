CREATE DATABASE mydb;
USE mydb 
CREATE TABLE localizacion (objectID INT PRIMARY KEY, rings MULTIPOLYGON);
CREATE TABLE atributos (HOJA INT, ID INT, DLO VARCHAR (1000), OBJECTID INT PRIMARY KEY, column1 INT);
INSERT INTO localizacion (resultado);
INSERT INTO atributos (resultado1);